[package]
name = "crabeam-cli"
version = "0.1.0"
edition = "2024"

[dependencies]
crabeam-host-core = { path = "../crabeam-host-core" }
anyhow = "1"
tokio = { version = "1.51.1", features = ["full"]}
